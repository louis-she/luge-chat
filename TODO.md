# 路鸽 (Luge) — 功能缺口与待办

> 基于当前代码完成度整理，按优先级分层。  
> 更新：2026-07-29（第二次路测结果已入库，见下文）

---

## 方案甲 — 火山 AI 音视频互动（当前主线）

目标：用火山 RTC「打电话」体验替换自研 ASR↔TTS；地理 / 足迹 / 配额仍由路鸽后端通过 **Function Calling** 提供。

```
手机 RTC SDK ←房间→ 火山（ASR + 方舟 LLM + TTS + 打断）
                         │ Function Calling
                         ▼
                   api.luge.chat（周边 POI / 足迹 / 配额）
```

### 任务拆解（按顺序击破）


| #        | 任务                                                     | 验收标准                                           | 状态  |
| -------- | ------------------------------------------------------ | ---------------------------------------------- | --- |
| **V0**   | 开通火山 RTC + 智能体；AppId / AppKey / 回调 / OpenAPI AK/SK | 凭证齐；模板 → `docs/ops/volcengine-startvoicechat-template.md`（TTS=佩奇 2.0 uranus） | [x] |
| **V0.5** | 能力调研沉淀本地文档                                             | → `docs/ops/volcengine-ai-av-research.md`      | [x] |
| **V1**   | Expo Dev Client 接入 `@volcengine/react-native-rtc`；真机进房 | 真机已验证：`onRoomStateChanged state=0` / 进房成功（音频） | [x] |
| **V2**   | 服务端发 RTC Token + `StartVoiceChat`（无 FC）                | 真机 ASR+对话已通；欢迎语+路鸽人设已挂                            | [x] |
| **V2.5** | VoiceChat 服务端回调（`api.luge.chat`）                        | RTS 日志 + FC；`answerFinish` 扣次（Round 去重）              | [x] |
| **V3**   | FC：`get_nearby_landmarks` → 现有高德/缓存逻辑                  | spike 上报 GPS + 回调 `Command=function` 回传（真机已通） | [x] |
| **V4**   | 回调扣配额 + 足迹写入                                           | Start 拦用尽；扣次 `answerFinish`；登录用户 asrFinish→足迹（待路测） | [~] |
| **V5**   | 路鸽启动/停止 = 进房/退房；关掉旧 ASR/HTTP TTS 主路径                   | 雷达真机默认 RTC；`EXPO_PUBLIC_LUGE_VOICE_PATH=legacy` 可回退 | [~] |
| **V6**   | 主动讲解 = `ExternalTextToSpeech`；佩奇猪音色对齐                  | RTC 播报 + Dev 地图候选 POI；真机路测 span 门槛              | [~] |
| **V7**   | 路测验收 A1/A2/打断/弱网                                       | 已跑一轮真机；遗留见「第二次路测」E～M                              | [~] |


**刻意后置**：MCP、视觉理解、火山自带 RAG/联网 Agent、Viking 长期记忆、云端录制、音色复刻。

**你还需要提供**：方舟 Endpoint ID（+ API Key）；确认已开通 AI 音视频互动（不只是 RTC 空房间）。

调研结论摘要：扣配额用 **服务端回调 `answerFinish`** 比客户端自报更合理；地理仍靠 FC，不要指望火山 RAG 知道车在哪。

---

## A1 按需说话窗（产品定案 · 2026-07-29）

> 为砍「Agent 音频」账单：默认不常开 `StartVoiceChat`；用户点按钮（将来可换唤醒词）再开「说话窗」。**实现未开工。**

| 项 | 定案 |
|----|------|
| 打开说话窗 | 用户点按钮 → `StartVoiceChat`（非唤醒词；唤醒词后置挂同一「开窗」） |
| 欢迎语 | **关掉**（`WelcomeMessage` 空或不发） |
| 关闭 | **空闲 30 秒**，或用户显式结束；**不以**单轮答完立刻关 |
| 空闲计时起点 | **播报/说话结束后才开始抠 30s**（见下「活动事件」）；进行中不计静默 |
| 跨两次按钮 | **允许忘**火山短期记忆（第一版） |
| 主动讲解 | **代开说话窗** → External TTS → 同样 30s 追问窗 |
| UI | Stop 后路鸽鸟「睡着」= 不可说话；醒着 = 说话窗开着 |
| Start 延迟 / 开关频率 / 扣次边角 | 第一版不纠结 |

**空闲 30s 用哪些结束节点（能拿到）：**

| 事件 | 来源 | 用途 |
|------|------|------|
| `answerFinish` | 火山 VoiceChat 回调（已有，扣次也用它） | **一轮问答播报结束** → 从此刻起算 30s |
| 用户又开说 / `beginAsking` / `asrFinish` | 同回调族 | 重置 30s（又在活动） |
| External TTS 播完 | 客户端 `speakExternal` Promise resolve；或回调里 TTS 相关 stage（实现时核对是否总有） | **主动讲解播完** → 起算 / 重置 30s |

原则：30s =「安静了 30s」，不是「answerFinish 后立刻关」。

**必做（实现时）：** 用户按钮与主动讲解代开窗 **互斥/排队**，禁止并发双 `Start` / 互相掐断。

**RTC 房 vs VoiceChat（概念，实现时再选挂法）：**

- **RTC 房间**：手机进房后的音视频管道（听得见对方、麦能推上去）。
- **VoiceChat 任务**：在这个房间上挂的「AI 智能体」（ASR+LLM+TTS+耳朵计费大头）。
- 今天 App「点路鸽」= **进房 + StartVoiceChat 绑在一起**，所以体感像同一个东西；API 上是两步，可以只停智能体、房间仍在（是否仍计 Agent 音频要以账单为准）。

**TODO（后置）**

- [ ] **跨说话窗灌历史**：下次 `Start` 时注入最近对话摘要/`ExternalPromptsForLLM`。第一版不做。
- [ ] 足迹 ≠ 多轮问答原文（灌历史是对话上下文）。

---



## 第一次路测问题（2026-07）

> 原始观察已归并成可执行项。方案甲落地后，A1/A2/C3 预期由 RTC 链路消化；B/C 文案类仍可能要补 prompt。



### A. 车载可用性（P0 — 不修就没法路测）


| #   | 问题                                 | 根因猜测                                     | 建议方向                       |
| --- | ---------------------------------- | ---------------------------------------- | -------------------------- |
| A1  | **车载蓝牙反复弹「通话中」**（亮 2～3 秒 → 灭 → 再弹） | 音频 session 在「通话 / 播放 / 录音」间来回切，不像一路保持通话态 | → **方案甲 V2/V5**（RTC 长通话）   |
| A2  | **锁屏 / 黑屏后语音断掉**                   | 仅前台；锁屏后 TTS/收音被系统挂起                      | → **方案甲 V2/V5** + 后台音频     |
| A3  | **朗读中（绿框）卡住，只能杀 App**              | TTS 结束回调丢失 / `isSpeaking` 未复位 / 麦抢占失败    | → **方案甲**后状态机简化；过渡期仍可加超时兜底 |




### B. 听感与朗读质量（P0）


| #   | 问题             | 根因猜测                                    | 建议方向                          |
| --- | -------------- | --------------------------------------- | ----------------------------- |
| B1  | **音量小，且越说越小**  | 豆包 TTS 增益 / session 音量叠加 / 分段播放衰减       | RTC 流式 TTS 后再验；过渡期可调 HTTP TTS |
| B2  | **语气跳变，像换人说话** | 分段合成、不同 chunk 韵律不连续；或 speaker 配置不稳      | → **方案甲**云端流式 TTS             |
| B3  | **念出「星星」等怪词**  | 模型输出 `*xxx`* / markdown，TTS 把 `*` 念成「星」 | prompt + 播报过滤（火山也有「播报时过滤」）    |
| B4  | **开头总要笑两声**    | system prompt「幽默」被模型执行为「哈哈」开场           | 方舟 system prompt 收紧           |




### C. 问答质量（P0/P1）


| #   | 问题                         | 根因猜测                        | 建议方向                    |
| --- | -------------------------- | --------------------------- | ----------------------- |
| C1  | **闲聊无回应**                  | 低意图判定过严，导游闲聊被 ignore        | RTC 全双工后重设；少本地 ignore   |
| C2  | **ASR 错字被当真（「右前方」→「有钱花」）** | 模型逐字回应错字，未结合地图/上下文纠错        | FC 地图上下文 + prompt 纠错    |
| C3  | **ASR 本身不准**               | 系统语音识别 + 车噪                 | → **方案甲** ASR + 降噪      |
| C4  | **地图有「新瓦村」，问了却答「金瓦村」**     | 地图标注与 LLM 上下文未对齐；或 ASR/纠错漂移 | → **V3** FC 强制带 map_hit |
| C5  | **同地追问重复「您正行驶在…车头对着…」**    | 每轮重塞 GPS/朝向，模型照念            | FC 按需查；prompt 禁重复方位     |
| C6  | **无聊打趣很尴尬**                | 「幽默」过火                      | system prompt 收紧        |




### D. 地图 / 足迹（P1 — 不挡主链路）


| #   | 问题                 | 根因猜测              | 建议方向                  |
| --- | ------------------ | ----------------- | --------------------- |
| D1  | **地图信息量少，缩近才有内容**  | 标注策略偏稀；缩放级别未按百度习惯 | 近距多标 POI / 村镇；远距只留关键点 |
| D2  | **足迹摘要人称「用户/你」别扭** | 摘要 prompt 用语      | 改成无人称叙述（「此行关注…」）      |




### 建议击破顺序（已切换）

1. **方案甲 V0 → V7**（通话体验 + 地理 FC）
2. 路测后扫尾 B3/B4/C5/C6（prompt）
3. D1/D2（地图密度、足迹文案）

---



## 第二次路测问题（2026-07-29）

> 真机有线/车载录测观察。主链路（RTC 被动问路）大体可用；缺口集中在 **prompt / 主动讲解上下文 / 方位与可见性 / 地图交互 / 候选类型**。  
> 编号 E 起，与第一次 A–D 区分。原始口述已整理成可执行项，细节后续可继续加工。


### E. 足迹 cue 与主动讲解话术（prompt）


| #   | 问题 | 观察 / 根因猜测 | 建议方向 |
| --- | ---- | --------------- | -------- |
| E1  | **同名类景点过度 cue 旧足迹** | 经过带「公园」的景点时，反复 cue「你以前去过北湖公园」；同字串（公园）+ 系统提示里带足迹上下文，容易错误关联。对比：经过岷江某点 cue 以前到过岷江另一处，这种关联合理 | **已做（2026-07-29）**：主动讲解注入足迹前按专名核心过滤；RTC/HTTP/主动讲 prompt 禁止泛类词串场 + 同通话最多 cue 一次；待路测体感 |
| E2  | **主动讲解套话每句都有** | 每句结尾类似「如果你感兴趣，可以放慢车速 / 打开车窗 / 走进去看看」——无信息量且重复 | **已做**：主动讲 / RTC / HTTP prompt 禁行动号召；兜底口播改为「这边是{名}。」 |
| E3  | **预设用户一定在开车** | 口播会出现「我们现在在开车」之类 | **已做**：人设改为旅途导游、中性位置表述；勿默认开车 |


### F. 主动讲解应进入完整对话（产品定案 · P0）

**现状：** 主动讲解与问路同处一个 RTC 房间；文案由 `luge-chat` 另写，经火山 **`ExternalTextToSpeech`** 播报。官方 FAQ 写明：**自定义播报文本会放入历史对话**（见 [传入自定义文本让 AI 播报 · FAQ Q2](https://docs.volcengine.com/docs/6348/1449206?lang=zh)）。  
因此「旁路 = 一定不进历史」的旧判断 **作废**。路测 F1（追问答成更早话题）更可能是：**历史里有稿，但优先级/话题锚定不够**（足迹 cue、prompt、HistoryLength、分段播报、追问时机等），而不是「火山根本没记」。

**定案（产品形态仍成立）：** 主动讲解应对用户表现为**同一段对话里的助手开口**，追问能接上刚讲的点。技术上可继续 External TTS（官方称会进历史），但要把「当前讲的是谁」钉死，必要时再叠加「文本提问 / 自定义指令」。


| #   | 问题 | 观察 / 根因猜测 | 建议方向 |
| --- | ---- | --------------- | -------- |
| F0  | **对话连贯性不足（非「完全没进历史」）** | External TTS **会**进历史（官方 FAQ）；F1 曾发生 → 话题锚定弱、旧足迹抢戏 | **已验收（2026-07-29）**：`ExternalPromptsForLLM` + prompt 锚定；静止用 Dev「立刻主动讲解」+ 追问通过 |
| F1  | **刚主动讲完立即追问，答的却是更早话题** | 与 E1 可能同源；或 History 有稿但模型没当成当前话题 | **已验收**：讲完立刻追问能指刚讲的点。E1 足迹乱 cue 仍可能抢戏，单独收 |

#### 火山能力核实（2026-07-29，已按 FAQ 修正）

| 官方文档 | 链接 | 对路鸽的含义 |
|----------|------|----------------|
| **传入自定义文本让 AI 播报** | [docs 6348/1449206](https://docs.volcengine.com/docs/6348/1449206?lang=zh) | ≈ 现用 `ExternalTextToSpeech`。文案不经主 LLM 生成，但 **FAQ Q2：播放内容会放入历史对话**。我们当前主路径。 |
| **传入文本直接提问** | [6348/2129096](https://www.volcengine.com/docs/6348/2129096) | 文本当作用户提问 → 主 LLM 生成再播；适合「让火山自己写口播」或更强的一轮对话形态 |
| **传递自定义指令** | [6348/2386107](https://www.volcengine.com/docs/6348/2386107) | 塞指令/背景；可用来钉死「当前 POI = xxx」，补 F1 |

**结论（修正后）：**

1. **能主动插入**；我们已在用的 External TTS **按官方说明会进历史**——产品定案「要在对话里」与现路径 **不必然冲突**。  
2. **F1 不能再用「没进历史」一句话解释**；优先查：prompt/足迹抢话题、HistoryLength、多段 External TTS、追问是否早于写入完成。  
3. **路径选择：**  
   - **保持 External TTS（默认）** + 强化「当前讲解 POI」锚定（prompt / 自定义指令）→ 成本最低。  
   - 若锚定仍失败 → spike **传入文本直接提问** 或 **自定义指令**。  
   - CustomLLM 仍是终极兜底，改造量大。  
4. **Spike 验收不变：** 主动讲完立刻追问 → 必须答刚讲的点。

**Spike 验收：** 主动讲完立刻问「它什么时候建的」→ 答的是刚讲的那个点，而不是更早话题。


### G. 被动问答卡死（~~P0~~ · 已修 2026-07-29）


| #   | 问题 | 观察 / 根因猜测 | 建议方向 |
| --- | ---- | --------------- | -------- |
| G1  | **问旁边的河/山会卡住不回答；闲聊（「你叫啥」）很快** | ~~被动介绍整体还行，但依赖 FC / 地理的问题易挂起~~ **根因（2026-07-29）：** `817fae1` 删掉 `formatLandmarksForLlm`，`volc-voice-callback` **worker 直接 boot 失败** → 地理 FC 全挂；闲聊不走 FC 所以仍通 | **已修并部署**：恢复导出；查周边 4.5s 超时仍回传；`UpdateVoiceChat` 失败时 ExternalTTS 人话兜底；OpenAPI fetch 10s 超时 |


### H. 方位、距离与「能不能看见」（P0/P1）


| #   | 问题 | 观察 / 根因猜测 | 建议方向 |
| --- | ---- | --------------- | -------- |
| H1  | **车头朝向判断仍差** | 介绍的景点开车时往往看不见（被挡或太远）；很多在前进方向的**背后** | **已做（2026-07-29）**：相对方位 8 向（含侧方/正后方）；FC 结果前方优先排序；RTC/HTTP/主动讲 prompt 强制用相对方位 |
| H2  | **缺少距离，用户无法判断是否值得扭头看** | 说「右前方有某某」用户会去看；若其实在背后 2km 或很远，白看 | **已做**：工具回传 `distance_spoken`；>150m 必须带大约距离；后方更要说清 |


### I. 地图：正在讲的点要醒目 + 跟镜（P1）


| #   | 问题 | 观察 / 根因猜测 | 建议方向 |
| --- | ---- | --------------- | -------- |
| I1  | **正在介绍的点不够醒目** | 用户不知道当前口播对应哪颗针 | **已做（2026-07-29）**：主动讲解开口时生产环境也渲染 speakFocus 高亮针（涟漪）；估时长后清除 |
| I2  | **正在讲的点可能在当前视野外** | 用户 zoom/拖过地图后，听得到但看不见点 | **已做**：开口时 animate 纳入用户+POI（过远则只跟 POI）；与 I1 联动 |


### J. 地图：拖动与回位（P1）


| #   | 问题 | 观察 / 根因猜测 | 建议方向 |
| --- | ---- | --------------- | -------- |
| J1  | **主地图不能拖动** | 交互受限 | **已做**：去掉受控 region / 常开 followsUserLocation；可拖可缩放 |
| J2  | **拖走后难回当前位置** | — | **已做**：暂停跟车 8s 后自动回位；暂停期间右下角「回位」按钮 |


### K. 候选类型过窄：桥 / 隧道 / 小镇（P1）


| #   | 问题 | 观察 / 根因猜测 | 建议方向 |
| --- | ---- | --------------- | -------- |
| K1  | **过桥、过隧道很少介绍** | ~~主动讲解目前偏「风景名胜」~~ 已把桥/隧/立交纳入黄点仓（190306/307/310），靠模型筛是否值得讲 | 路测验收：过桥/隧是否会开口；噪声过多再收紧 |
| K2  | **过小镇/城镇也少介绍** | 已纳入乡镇/村地名（190106/190108） | 同上 |


### 第二次路测 — 当前优先级（2026-07-29 更新）

**已收（不必再排）：** G1 · F0/F1 · E1/E2/E3 · H1/H2 方位距离 · J1/J2 · I1/I2 · 高德刷量/风景库 · 问路场景×语意半径 · 主动讲解场景半径+选点 · 找不到时短答 · 黄点 types · Dev「立刻主动讲解」  

| 优先级 | 项 | 为什么现在做 |
|--------|----|--------------|
| **P2** | **K1 / K2** 桥/隧/小镇 | 候选已进仓，待**开车**验收会不会乱讲 |

**建议下一包：** K 路测验收；或 MVP 旁路（支付 / V7 车载长测）。

**MVP 旁路（不挡路测体验，上架前要动）：** 真支付 · `public` schema · 发布前稳定性扫尾 · V7 车载 1h（蓝牙/锁屏/打断）

### 高德刷量止血（2026-07-29）

- [x] **客户端风景库（整库替换）**：距上次搜索中心 ≤ 半径×50% 不重搜；出圈用新结果**替换**整库  
  - 黄点预览 + 主动讲解共用 `lib/scenicAroundCache.ts`；正式讲可带 `cached_scenic_candidates` 跳过服务端 around  

### 问路半径：场景 × 语意（2026-07-29）

- [x] 不再固定 3km；公式与默认权重见 `docs/product/core-flows.md` §4.4.1  
- [x] 高级设置可调三档底径 + 五个语意倍率；随 GPS 写入 `geo_radius_prefs`  
- [x] 水系/山岳问法走 `natural_geo`，泛化词不进 keywords  
- [x] **主动讲解**也用场景底径（不乘语意）；候选列表带方位+距离；prompt 权衡优质与近处  

---



## MVP 闭环版（当前冲刺）

目标：**真机路上能连续用 1 小时、问得动、记得住、用完了能付钱**——不扩新功能面，先把主链路跑通。


| 状态  | 项              | 说明                                                            |
| --- | -------------- | ------------------------------------------------------------- |
| [~] | **真机路测 1h**    | 2026-07-29 已跑一轮；遗留见「第二次路测」E～K；F0/F1：External TTS **会进历史**（火山 FAQ），追问错题先查话题锚定 |
| [x] | **错误足迹改标题 v1** | 详情页「改标题」→ RPC `set_footprint_title`；仅改 `title`，不动 POI/坐标      |
| [x] | **配额用尽 → 付费页** | 弹窗跳转 `/pay?from=exhausted`；内测模拟购买充值 `balance_asks`；购买后刷新次数并返回 |
| [ ] | **支付接入（至少一项）** | Apple IAP 或微信支付；替换当前模拟购买                                      |
| [ ] | **生产 schema**  | `dev` 验证通过的迁移推到 `public`，Release 构建默认连生产                      |
| [ ] | **发布前稳定性扫尾**   | 技术错误不朗读、弱网重试文案、`__DEV__` 日志不上屏                                |


**刻意不做（MVP 之后）**：后台定位与省电、对话回放、足迹合并/拆分、地理沉淀 v2.1–v2.3、商店全量上架合规包。

---



## 产品阶段选择（先对齐主线）

接下来 1～2 个月更想验证哪件事？选定后调整下方优先级。


| 主线          | 含义           | 建议优先做的模块           |
| ----------- | ------------ | ------------------ |
| **A. 问答体验** | 用户愿不愿意在路上一直问 | 路测闭环、语音链路、弱网/蓝牙    |
| **B. 足迹记忆** | 足迹会不会让人想回来看  | 总览地图、对话回放、Visit 详情 |
| **C. 商业化**  | 愿不愿意付钱       | 支付接入、配额用尽链路、生产环境   |


---



## P0 — 核心体验尚未闭环

路鸽与「普通语音助手」的本质差别；若目标是「自驾导游」，迟早要上。

- [x] **主动讲解 v1：距离 + 时间双门槛（span）**
  - 已实现：路鸽启动后建立位移锚点；**距锚点 ≥20km 且距上次查询 ≥10min** 才打一次高德 regeo（不扫周边 POI）；LLM 判定是否开口；**距上次朗读 ≥15min** 才真正 TTS
  - 客户端：`lib/proactiveSpan.ts`、`lib/useProactiveGuide.ts`；服务端：`luge-chat?mode=proactive`
  - 开发调试：`.env` 可设 `EXPO_PUBLIC_PROACTIVE_MIN_DISTANCE_KM=2` 等缩小阈值
- [x] **地理知识沉淀 v1：高德命中 → 候选信号 → 达门槛升格共享缓存**
  - `geo_landmark_candidates` 聚合信号；升格分数 = **提问×1 + 收藏×2**，**≥5 分** 写入 `geo_landmarks_cache`
  - 已升格后后续问答更新共享讲解；未登录不计入升格信号
  - `dev.geo_landmarks_cache` + RPC `nearby_geo_landmarks` / `upsert_geo_landmark_amap`
  - `luge-chat`：问答前先撞本地缓存；高德 POI 命中且回答完成后写入/更新 `ai_formatted_story`
  - 同 `amap_poi_id` 聚合 `hit_count`；收藏足迹仍为远期升格信号
- [x] **主动讲解 v2：高德 POI 为主 + 行为沉淀**
  - 前期**不预建**全国地理节点库；主动讲解改为复用统一链路：先撞地理缓存，再看高德全地理 POI
  - 主动讲解**不会自动写入用户足迹**；只有用户后续主动追问并继续深聊同一 POI，才允许进入足迹
  - 主动讲解返回命中 POI，上下文会透传到下一轮问答，供足迹分类器判断是否属于“延续深聊”
- [ ] **后台定位与省电**
  - 现状：仅前台 `watchPosition`；App 切后台不会继续 span
- [ ] **地理知识沉淀 v2.1：按 POI 类型做过期续期**
  - 现状：缓存升格与读写已接通，但 `valid_until` 基本还是统一策略
  - 目标：山川/河流/桥梁/古镇等稳定地理对象延长续期；时效性强的信息维持较短 TTL
- [ ] **地理知识沉淀 v2.2：优化升格权重与质量控制**
  - 现状：当前仅用 `提问×1 + 收藏×2 ≥ 5` 的统一门槛，足够 MVP，但较粗
  - 目标：区分高质量/低质量信号，避免脏数据过快升格；必要时按 POI 类型调整阈值
- [ ] **地理知识沉淀 v2.3：主动讲解与共享缓存的关系再收口**
  - 现状：主动讲解已复用统一 POI 检索链路，但默认不额外计升格分
  - 目标：等真实路测后再决定主动讲解命中是否计弱信号、是否参与续期



### 主动讲解 span 设计（已采用）

```
查询门槛（同时满足）          开口门槛（额外）
─────────────────────        ─────────────────
移动 ≥ 20km（从上次锚点）      距上次朗读 ≥ 15min
且 距上次查询 ≥ 10min         且 LLM 判定 worth speaking
```

城里 POI 再多，也不会每几百米打一次高德；高速上也不会 10 分钟连讲三次。

---



## P1 — 足迹模块：骨架已有，记忆感偏弱

详情页（标题、描述、地图、Visit 时间线）已上线；以下让足迹从「摘要卡片」变成「可重温的旅途记忆」。

- [x] **足迹收藏（个人筛选 + 升格信号）**
  - 详情页「收藏 / 已收藏」；列表筛「全部 / 收藏」；无打分、无爱心图标
  - DB：`user_footprints.favorited_at` + RPC `set_footprint_favorite`
  - 升格规则（远期）：收藏×2 + 提问×1，满 5 分写入共享地理缓存
- [x] **足迹总览地图**
  - 现状：列表页「足迹地图」仍为占位卡片
  - 目标：一张图展示所有已点亮地点，成就感强、实现成本相对低
- [ ] **对话回放**
  - 现状：「重新播放」未接入
  - 目标：按 Visit 或整段足迹，TTS 重播历史 Q&A
- [x] **Visit 时间线展开完整对话**
  - 现状：每条 Visit 仅显示 LLM 摘要
  - 目标：点进某次到访可查看当时每一轮问答原文
- [x] **足迹标题随多次 Visit 动态更新**
  - 首次 visit 会生成标题；后续每次 visit 摘要时，都会结合「已有标题 + 本次对话」判断是否更新标题
  - 现状：新增/活跃足迹已生效；历史老足迹若没有新对话触发，仍需一次性回填
- [x] **错误足迹手动修正 v1：改标题**
  - 详情页「改标题」；RPC `set_footprint_title`（仅 `title`，最长 80 字，校验 owner）
  - 不改 `poi_name` / 坐标 / 合并关系
- [ ] **错误足迹合并 / 拆分**
  - 进阶：两条足迹合成一条、或一条拆成两条（分类器误合并时）
- [ ] **旧数据回填（一次性）**
  - 标题动态更新、坐标解析已修，但历史足迹若没有后续对话，仍不会自动重算
  - 可选：跑一次性脚本，对已有 `user_footprints` 批量重新摘要 / 校正标题

---



## P1 — 商业化与发布

明显空白；若 1～2 个月内要内测收费或上架，应与路测并行。

- [x] **配额用尽 → 付费页（模拟购买）**
  - 弹窗「查看次数包」→ `/pay?from=exhausted`；`quota` Edge `mock_purchase` 充值 `balance_asks`
  - 购买成功后刷新配额 + 个人中心次数；`继续问路` 返回雷达页
- [ ] **支付接入**
  - 现状：`app/pay.tsx` 有 UI；`profile` 标注「支付待接入」
  - 需要：Apple IAP、微信支付（或先上一项）
- [ ] **配额用尽 → 付费完整链路**
  - 现状：配额逻辑已有
  - 需要：真机上「用完 → 弹窗 → 购买 → 恢复次数」全流程走通
- [ ] **生产 schema（**`dev` **→** `public`**）**
  - 现状：开发默认连 `dev` schema
  - 需要：迁移验证后推 `public`，Release 构建指向生产
- [ ] **商店与合规**
  - Release 构建、App Store / 安卓商店、隐私政策、备案相关文案与权限说明

---



## P1 — 真机路测闭环

不适合在办公室猜，需专门找高速 / 国道场景验证。

- [ ] **路测清单（至少跑通 1 小时连续使用）**
  - 真机 HTTPS、弱网 / 隧道断连、请求重试与错误提示
  - 车载蓝牙、歌单 / 环境噪音下的收音与误触发
  - TTS 朗读中用户插话：打断与恢复策略
  - 配额扣减与刷新、用尽后的 UX
- [ ] **Android 地图与朝向**
  - 现状：iOS 已依赖系统蓝点；Android 仍为自定义 Marker，朝向能力弱于 iOS
- [ ] **后台定位与省电**
  - 若做主动讲解，需 `expo-location` 后台模式与电量策略；当前以前台问答为主

---



## P2 — 语音与对话链路（持续打磨）

部分已做（嗯 / 噪音过滤、技术错误不朗读、判定模型与回答模型分流），路上仍会暴露新问题。

- [ ] **收音误触发**：背景音乐、乘客闲聊、键盘声（已去掉蓝色波形态，策略仍需路上调）
- [ ] **弱网 / 离线**：超时重试、排队、友好提示（避免朗读 exception 等技术词，已部分修复）
- [ ] **全双工体验**：边说边听、打断 TTS 后立即开麦的时序
- [ ] **判定层成本与准确率**：`DEEPSEEK_JUDGE_MODEL` vs `DEEPSEEK_ANSWER_MODEL` 在真实路况下的漏判 / 误判

---



## P2 — 雷达页与日常 UX

- [ ] **路鸽运行时状态更清晰**：思考 / 说话 / 静音 / 收音（鸽子 icon 状态已迭代，可继续统一文案与反馈）
- [ ] **开发调试仅留在 Metro 终端**：prompt / debug 日志不上屏（已要求，发布前再扫一遍 `__DEV__`）
- [ ] **模拟器 vs 真机**：Dev 定位面板、文字问路栏仅模拟器；真机体验需单独验收清单

---



## P3 — 架构与运维（非用户可见，但影响迭代速度）

- [ ] **Edge Functions 部署流程文档化**：`luge-chat`、`footprint-jobs`、`wechat-auth` 等同步与重启
- [ ] **足迹定时任务**：`footprint-jobs` cron（10min 摘要 + 24h 归档）线上监控与失败告警
- [ ] **LLM 模型配置**：判定 / 回答 / 摘要 / 分类分模型，成本与延迟权衡
- [ ] **高德 Key 类型**：须 Web 服务 Key，非 Android/iOS SDK Key（见 `docs/ops/supabase-production.md`）

---



## P3 — 愿景功能（可明确暂缓）


| 功能        | 说明                        |
| --------- | ------------------------- |
| 足迹分享 / 导出 | 生成可分享的旅途片段或日记             |
| 多用户 / 社交  | 与当前单人自驾场景关系不大             |
| 包天畅聊等套餐   | `pay.tsx` 已标「即将推出」，依赖支付基建 |


---



## 建议的「若只能做 3 件事」

1. **一条路测闭环**：真机 + 弱网 + 蓝牙 + 配额用尽 → 付费页，完整用满 1 小时无断档
2. **足迹总览地图**：所有点亮 POI 落在一张地图上
3. **对齐是否做主动讲解**：做 → 排期撞库与后台定位；不做 → 集中把「随时问路」磨稳

---



## 已完成（近期，供对照）

- [x] `**river-chat` → `luge-chat` 全面更名**（Edge Function、客户端 `lib/lugeChat.ts`、`askLugeGuide`、API 字段 `map_hit`、Prompt 去河流中心表述）
- [x] 主动讲解 v1（距离 + 时间 span、轻量 regeo、LLM 判定开口）
- [x] 地理缓存 v1：`geo_landmarks_cache` 读写接入 `luge-chat`（高德 POI 问答后沉淀，非预建库）
- [x] 足迹列表 → 详情页（标题、描述、地图坐标、Visit 时间线倒序）
- [x] 坐标 EWKB 解析 + Visit GPS fallback
- [x] 足迹标题摘要 prompt（用户关注点优先于楼盘名等）
- [x] 低意图语音过滤（客户端 + 服务端判定）
- [x] LLM prompt 日志仅 Metro 终端
- [x] 判定 / 回答可分模型（`DEEPSEEK_JUDGE_MODEL` / `DEEPSEEK_ANSWER_MODEL`）
- [x] 足迹总览地图（`FootprintsOverviewMap`，列表页全宽可拖动）
- [x] 错误足迹改标题 v1（`set_footprint_title` + 详情页编辑）